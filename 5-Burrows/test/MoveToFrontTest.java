/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

import org.junit.Test;
import static org.junit.Assert.*;

/**
 *
 * @author sakhnik
 */
public class MoveToFrontTest {
    
    public MoveToFrontTest() {
    }

    //@Test
    //public void testEncoder() {
    //    MoveToFront.Encoder encoder = new MoveToFront.Encoder();
    //    assertEquals(0x41, encoder.encode('A'));
    //    assertEquals(0x42, encoder.encode('B'));
    //    assertEquals(0x52, encoder.encode('R'));
    //    assertEquals(2, encoder.encode('A'));
    //    assertEquals(0x44, encoder.encode('C'));
    //    assertEquals(1, encoder.encode('A'));
    //    assertEquals(0x45, encoder.encode('D'));
    //    assertEquals(1, encoder.encode('A'));
    //    assertEquals(4, encoder.encode('B'));
    //    assertEquals(4, encoder.encode('R'));
    //    assertEquals(2, encoder.encode('A'));
    //    assertEquals(0x26, encoder.encode('!'));
    //}

    //@Test
    //public void testDecoder() {
    //    MoveToFront.Decoder decoder = new MoveToFront.Decoder();
    //    assertEquals('A', decoder.decode(0x41));
    //    assertEquals('B', decoder.decode(0x42));
    //    assertEquals('R', decoder.decode(0x52));
    //    assertEquals('A', decoder.decode(2));
    //    assertEquals('C', decoder.decode(0x44));
    //    assertEquals('A', decoder.decode(1));
    //    assertEquals('D', decoder.decode(0x45));
    //    assertEquals('A', decoder.decode(1));
    //    assertEquals('B', decoder.decode(4));
    //    assertEquals('R', decoder.decode(4));
    //    assertEquals('A', decoder.decode(2));
    //    assertEquals('!', decoder.decode(0x26));
    //}
}
